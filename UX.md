# kakeibo — UXの現状

`apps/web/index.html`(1ファイルSPA)の実装を、画面・導線ベースでそのまま記述したもの。

**DESIGN.mdの「5. UX方針」との違い**: あちらは当初の設計思想(日単位ドリルダウン、記録漏れの自動検出など)。
このファイルはその後の実装で実際にどう動いているかの記録で、方針と食い違う箇所は
「10. 当初のUX方針との差分」にまとめてある。UXを変更する作業をするときは、まず
このファイルで現状を把握し、変更後はここを更新すること。

## 1. 全体構造

```
<div id="root"> 1つに毎回全画面分のHTMLを書き込んで再描画(仮想DOMなし)
├─ 装飾SVG(固定背景、雲/波)
└─ .app-shell
   ├─ header(ロゴ + ナビタブ)
   ├─ main(ページごとのコンテンツ)
   └─ ノード管理モーダル(managerOpen が true の間だけ)
```

状態は2系統に分かれる。

- **`state`**: `go(page)` のたびに丸ごと差し替わる(前のページの入力途中の状態などを
  持ち越さないため)。`state.page` / `state.detailNodeId` / `state.spendPeriod` /
  `state.analysisPeriod` / `state.txSearch` / `state.editLine` など
- **モジュールレベル変数**: ページ遷移をまたいで保持される(`chartMode` /
  `nodeViewMode` / `hiddenNodeIds` / `netWorthHidden` / `recordSlot` /
  `selectedFromNode` / `txPeriod` / `txListRows` のキャッシュなど)。セレクタの
  表示とキャッシュ済みデータの整合を保つ必要があるものはこちら側に置いている

## 2. ナビゲーション

- **ヘッダー左**: ロゴ「Budget Sandbox」(クリックでダッシュボードへ)
- **ヘッダー右**: ログイン中のメールアドレス表示、「構成比」「設定」(ナビタブには
  含まれない、下線付きテキストリンク)
- **ナビタブ(4つ、常時表示)**: ダッシュボード / 記録 / 取引一覧 / ノード一覧
  - 「ノード一覧」タブはノード詳細ページ(`detail`)でも点灯したままになる
    (詳細はノード一覧の子画面という扱い)
- **構成比・設定**はタブに属さない独立ページで、上部の「← 戻る」でどのページから
  遷移してきたか(`prevPage`)に戻る。戻り先が構成比/設定自身だった場合(通常は
  起こらない)は記録画面にフォールバックする

## 3. 画面ごとの詳細

### ログイン

メール・パスワードのみ。サインアップ導線はこの画面にはない(`apps/cli/signup.js`
が別途ある)。エラーはメッセージをそのまま表示。

### ダッシュボード(既定の着地ページではない — 既定は「記録」)

- 「◯年◯月」の見出しと、当月1日〜今日までの集計期間ラベル
- 統計カード3枚: 純資産(個別マスク切替あり) / 今月の支出(赤系) / 今月の収入(青系)
- 「純資産の推移」: 直近30日の棒グラフ(日毎の値ではなく `v_net_worth_daily` の推移)
- 「消費相手 上位」: 期間セレクタ(今月/先月/直近30日/全期間、`state.spendPeriod`)
  付きで、flowの葉ノードのうち残高が正のものを金額順に上位5件。各行にプログレス
  バーとノード単位マスクトグルが付く
- 「最近の記録」: 直近5件、取引一覧と同じ行コンポーネント(編集・削除ボタン付き)
- 右上「記録する」ボタンで記録画面へ

### 記録(ログイン後の既定ページ)

- 「出どころ → 行き先」のスロットが横並びで表示され、片方をクリックするとその
  スロットが「候補選択の対象」になる(`recordSlot`)
- 候補チップ: 出どころは資産・負債の葉ノード全件(name順)、行き先は
  `v_node_suggestions`(直近30日の使用頻度順)上位8件
- チップ下の検索ボックスでノード名/パスを検索(`searchNodes`)、結果をクリックで
  選択。出どころを選ぶと自動的に行き先スロットにフォーカスが移る
- 金額・日付(既定は今日)・メモを入力して「登録」。編集中(取引一覧・最近の記録
  からの「編集」ボタン経由)は「更新」表示になり「キャンセル」ボタンが出る
- 出どころは登録後も選択状態を保持する(同じ財布から続けて記録することが多いため)。
  行き先は毎回リセットされる
- 「今日の記録」に当日分の一覧を表示。「ノードを追加」リンクでノード管理モーダルを開く

### 取引一覧

- 期間セレクタ(今月/先月/直近30日/全期間、既定は全期間)+ フリーテキスト検索
- 支出/収入/振替の凡例と、各行の日付セルに色ドット・金額セルに色文字
  (資産・負債↔flowの向きから機械判定。それ以外はすべて「振替」に丸める)
- 20件ずつページネーション、「さらに読み込む」ボタンで追加取得
- 検索はサーバーへの問い合わせではなく、**その時点で読み込み済みの行**に対する
  クライアント側のフィルタ(from_name/to_name/memoの部分一致)
- 期間セレクタを変更するとキャッシュを破棄して1ページ目から再取得する

### ノード一覧

- 表示切替: リスト / カード(`nodeViewMode`、localStorage永続化)
- 「アーカイブ済みを表示」トグル
- **リスト表示**: 資産 / 負債 / 消費相手(多い順、flowの葉ノードで残高が正のもの) /
  支出・収入(階層)の4セクション。資産・負債・支出収入セクションは折りたたみ式
  ツリー(既定は最上位だけ展開、子を持つ行に▸/▾)
- **カード表示**: 全ノードを残高の絶対値が大きい順に1グリッドで並べる。各カードに
  種別バッジ・スパークライン(残高推移の折れ線)・ミニ進捗バーが付く
- ノードごとにマスクトグル(🐵/🙈)、葉ノードのみアーカイブボタン(📦)が出る
- 「ノードを作成」でノード管理モーダルを開く。行/カードのクリックでノード詳細へ

### ノード詳細

- パンくず(「ノード一覧 / ノード名」)
- 残高(全期間の累計、マスク可)+「累積推移」/「1日あたり推移」の切替棒グラフ
  (ゼロ軸、正が上・負が下、`chartMode` で永続化)
- 「内訳」に、このノードと子孫に関わる取引を新しい順で全件表示

### 構成比

- 期間セレクタ(今月/先月/直近30日/全期間)。**支出・収入構成比のみ**この期間が
  効き、**資産・負債構成比は常に現在残高**(期間指定の影響を受けない)。この非対称性
  は各カードの小見出しと、ページ上部の注記で明示している
- 対象はルート直下の flow(支出/収入)・asset/liability(純資産)ノードのみ。
  `exclude_from_flow_totals` なノードは含まない
- 4カード(支出構成比/収入構成比/純資産構成比(資産)/純資産構成比(負債、0件時は
  非表示)): 合計金額 + ドーナツチャート(ホバーで金額と割合をツールチップ表示) +
  凡例(ノード名・金額・割合・マスクトグル)
- 個別ノードを非表示にすると、そのカードの合計も一緒にマスクされる
  (合計から他の項目を引いて非表示額を逆算できてしまうのを防ぐため)

### 設定

- **アカウント**: メールアドレス表示のみ(変更不可)。表示名は入力してもこの端末の
  localStorageに保存されるだけ(バックエンドに保存されない、他端末には出ない)
- **表示**: 「通貨(JPY ¥)」「月の開始日(1日)」は表示専用で変更不可。
  「既定の出どころ」(記録画面の初期選択、この端末のみ)と「グラフの既定表示」
  (ノード詳細の累積/日次、`chartMode` と共有)は変更可能
- **データ**: CSV書き出し(全件をページングして取得しUTF-8 BOM付きCSVをダウン
  ロード、数式インジェクション対策あり)/ CSV取り込み(未実装、押すとアラートが
  出るだけ)/ ログアウト(赤枠のボタン、確認ダイアログなし)

### ノード管理(モーダル)

- 「記録」または「ノード一覧」の上に重ねて開く(閉じると元の画面に戻る)。
  中央固定配置(実装はこの1パターンのみ。スライドオーバー等の別レイアウトは無い)
- 新規作成: 親ノード(任意)/種別(flow・asset・liability、親を選ぶとその種別に
  固定される)/ノード名/「収支の集計・構成比から除外する」チェックボックス
  (flow選択時のみ表示。初期残高ノード用)
- 編集: 種別は変更不可(トリガーで守られている階層制約に関わるため)。名前・親・
  除外フラグのみ変更可
- 記録のある葉ノードを中間ノードに昇格させようとすると「階層化の確認」が挟まり、
  既存の記録を「その他(◯◯)」へ退避してから階層化する(`promoteToParent`)
- 下部に既存の階層を折りたたみツリーで表示(検索・絞り込みなし)。各行から「編集」
  「削除」に直接ジャンプできる

## 4. 横断的なUIパターン

| パターン | 使われる場所 |
|---|---|
| カード + `.card-head`(見出しと右側要素を横並び) | ほぼ全画面 |
| スロット選択 → 候補チップ → 検索 | 記録画面のみ |
| 折りたたみ式ツリー(既定は最上位だけ展開) | ノード一覧、ノード管理 |
| マスクトグル 🐵/🙈(ノード単位・純資産単位) | ダッシュボード、ノード一覧、ノード詳細、構成比 |
| アーカイブ 📦(葉ノードのみ) | ノード一覧 |
| 期間セレクタ(今月/先月/直近30日/全期間) | ダッシュボード(消費相手)、取引一覧、構成比 — 3箇所独立、連動しない |
| 支出/収入/振替の色分け | 取引一覧のみ(色ドット+文字色、専用の列は増やさない) |
| ページネーション(明示的な「さらに読み込む」) | 取引一覧のみ |
| ネイティブ `title` 属性によるホバー情報 | 棒グラフ・構成比ドーナツ・分類ドット |

## 5. ビジュアルデザイン

- テーマ名は「夏(Natsu)」。`.web-ui-ref/Budget Sandbox - Natsu.html`
  (株式会社コモレビ風の参照モック)を踏襲した青空グラデーション + 白い雲/波の
  固定SVG装飾
- フォント: 見出し(h1〜h3)と本文の `<p>` は Shippori Mincho(明朝)、それ以外の
  UIテキストは Noto Sans JP、金額・日付など数値は IBM Plex Mono
- 配色は `:root` のCSS変数(`--accent` 等)に一元化。支出=赤茶、収入・強調=青、
  振替・収支バッジ=淡い青紫
- レスポンシブは最小限: 640px以下で取引一覧の列(行き先・操作列)を間引くのみ。
  基本的にPC/タブレット幅を想定したレイアウト(`max-width:1080px` 中央寄せ)

## 6. 永続化(localStorage、すべて `kakeibo:` プレフィックス)

| キー | 内容 |
|---|---|
| `chartMode` | ノード詳細の既定グラフ種別(累積/日次) |
| `nodeViewMode` | ノード一覧の表示形式(リスト/カード) |
| `hiddenNodeIds` | マスク中のノードID一覧 |
| `netWorthHidden` | 純資産統計のマスク有無 |
| `defaultFromNodeId` | 記録画面の出どころ初期値 |
| `displayName` | 設定画面の表示名(この端末のみ、バックエンドには送られない) |

すべて「この端末のみ」の表示設定で、サーバー側の状態やRLSには影響しない。

## 7. PWA・オフライン

インストール可能(`manifest.webmanifest` + `sw.js`)。Service Workerは同一
オリジンのアプリシェル(`index.html` / `packages/core/*.js` / アイコン)だけを
network-first でキャッシュし、オフライン時はキャッシュにフォールバックする。
Supabase・Google Fonts・esm.sh へのリクエストはキャッシュ対象外なので、
**オフラインでは新しいデータの閲覧・記録はできない**(あくまで「シェルだけは
すぐ開ける」までの対応)。

## 8. セキュリティ・プライバシーがUXに現れる部分

- memoは端末上で復号(`crypto.js`)して表示される。暗号化キーはこのファイル内に
  ハードコードされており、狙いはSupabaseダッシュボード(RLSを回避できる管理者
  ロール)からmemoを読めなくすることで、ページのソースを読む相手からは隠せない
- マスクトグル(🐵/🙈)は「見た目を隠す」だけのUI機能で、認証や暗号化ではない。
  同じブラウザ・同じログイン状態なら誰でも解除できる

## 9. 未実装・既知の制限

- CSV取り込み(設定画面のボタンはアラート止まり)
- 通貨・月の開始日は設定画面に項目があるが実際は変更不可(JPY・1日固定)
- ノード管理モーダルの階層一覧に検索・絞り込みがない(ノード数が増えると縦に
  長くなる)
- 取引一覧の検索は「サーバー全体」ではなく「読み込み済みの行」だけが対象
- サインアップ導線がWeb UIにない(`apps/cli/signup.js` のみ)
- ログアウトに確認ダイアログがない

## 10. 当初のUX方針との差分(DESIGN.md「5. UX方針」)

| DESIGN.mdの構想 | 実装の現状 |
|---|---|
| ホームは「今日」から始まり、日をまたぐと自動遷移。取引リストは主役ではない | ダッシュボードは月次サマリ。「取引一覧」は独立したナビタブの主要画面 |
| ノード詳細のヘッダ数値: `flow` は「直近30日の増減」 | 実装は「全期間の累計」(`getCumulative` の最新値) |
| 記録漏れへの対処: 日次締めで財布残高を入力し、差分を「不明(現金)」ノードへの流出として自動計上する仕組み | 未実装。手動で取引を記録する以外の乖離検出手段はない |
| 木を辿るUIは管理・整理のときだけ | 実装はおおむね一致(記録画面は検索・サジェストのみ、木構造はノード一覧・ノード管理のみ) |

この差分自体は問題ではなく、実装が進む過程で優先順位が変わった結果。ただし
冒頭に書いた通り、今後UXに手を入れる際はこの表を見て、方針に寄せるか・現状を
追認してDESIGN.mdを更新するかを都度判断すること。
