# kakeibo — 設計書

## 1. 思想

### 統一抽象としてのノード

財布・銀行・クレジットカード・マクドナルド・バイト先・食費を、すべて等価な「ノード」として扱う。
各ノードは以下を共通して満たす。

- 累積残高が定義される
- 傾き(増減ペース)が定義される
- 任意期間の差分が定義される
- 取引の端点になれる

これにより、残高の計算式は1つで済み、ノード詳細画面は1種類で済む。
クレジットカード・ポイント還元・運用損益・振替・立て替えといった、
通常の家計簿アプリが個別対応する要素を、すべて同じ構造で表現できる。

### 累積を保持し、期間は後から切る

月次などの集計粒度を事前に固定しない。
累積残高 `B(t)` を保持しておけば、

- 傾き `dB/dt` = その時点の支出/収入ペース
- 差分 `B(t2) - B(t1)` = 任意期間の増減

が同じデータから導出できる。「今月」も「直近30日」も「特定の3日間」も同じ操作で出る。

### ゼロサム(閉包性)

1取引は必ず2つのノード間の移動として記録する。
`from_node` / `to_node` の2列構造なので、金額の保存はスキーマレベルで自動的に保証される。
検証用の制約やトリガーは不要。

### 拡張はノード追加で吸収する

新しい決済手段・新しい支出先・新しい収入源が増えても、
テーブル構造を変えずに `nodes` に行を追加するだけで対応する。
分類を細かくしたい場合も、既存ノードの下に子を作るだけで済む。

---

## 2. スキーマ

### nodes

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID PK | `gen_random_uuid()` |
| user_id | UUID FK → auth.users | RLS用 |
| name | TEXT NOT NULL | 財布、マクドナルド、その他(食費) |
| parent_id | UUID FK → nodes | 親ノード。ルートは NULL |
| node_type | TEXT NOT NULL | `asset` / `liability` / `flow` |
| currency | TEXT NOT NULL DEFAULT 'JPY' | 将来のマイル・外貨用 |
| is_archived | BOOLEAN DEFAULT false | 使わなくなったノードを隠す |
| created_at | TIMESTAMPTZ DEFAULT now() | |

**node_type の意味**

| 値 | 意味 | 純資産に含める | 既定表示 |
|---|---|---|---|
| `asset` | 実在する資産・債権 | ○ | 現在残高 |
| `liability` | 実在する債務 | ○(負の値として) | 現在残高 |
| `flow` | 通過点。累積は履歴 | × | 直近30日の増減 |

- 立て替えは `asset`(返してもらう権利は資産)
- クレジットカードは `liability`
- マクドナルド・バイト先・食費は `flow`

**階層を持つ。** `parent_id` による自己参照で木構造を作る。

```
食費
├── その他(食費)
├── 外食
│   ├── その他(外食)
│   └── マクドナルド
└── スーパー
    ├── その他(スーパー)
    └── スーパーA
```

**記録先は必ず葉ノード。** 中間ノード(食費、外食)には直接記録しない。
分類しきれない分は、各階層に `その他(...)` という葉ノードを置いてそこに記録する。

制約:

| 制約 | 実装方法 |
|---|---|
| 子は親と同じ `node_type` | トリガー(INSERT/UPDATE 時に親を参照して検証) |
| 循環参照を許さない | トリガー(再帰で祖先を辿り、自分が現れたら拒否) |
| `lines` の端点は葉ノードのみ | トリガー(その node_id を親に持つ行の存在を確認) |
| `parent_id` は同一 user_id のノードのみ | トリガー、または RLS 併用 |

いずれも外部キーや CHECK 制約では表現できないため、トリガー関数として実装する。
トリガーは `supabase/migrations` に定義し、アプリケーション側では検証しない
(DB を唯一の防衛線にする)。

**中間ノード化の扱い。** 葉ノード A に既に記録があり、その下に子を作りたくなった場合、
A は中間ノードになるため既存の記録が制約違反になる。手順は次の通り。

1. `A > その他(A)` という葉ノードを新設する
2. A を端点に持つ既存の `lines` を、`その他(A)` に付け替える
3. その後、他の子ノードを追加する

この操作は `packages/core` の `nodes.js` に `promoteToParent(nodeId)` として実装し、
手動での付け替えミスを防ぐ。

**粒度は後から上げられる。** 「その他(外食)」に溜まった記録のうち、
特定の店が増えてきたら、新しい葉ノードを作って `lines` の `node_id` を差し替える。
金額も日付も変わらないため、過去の集計は壊れない。

**中間ノードに子を追加するとき、既存の記録は動かさなくてよい。**
「外食」の下に「マクドナルド」を新設しても、既に「その他(外食)」にある記録はそのまま。

### lines(取引)

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID PK | |
| user_id | UUID FK → auth.users | RLS用 |
| occurred_on | DATE NOT NULL | 取引が起きた日 |
| from_node | UUID FK → nodes NOT NULL | 出どころ |
| to_node | UUID FK → nodes NOT NULL | 行き先 |
| amount | NUMERIC(14,2) NOT NULL CHECK (amount > 0) | 常に正 |
| memo | TEXT | 「その他」ノードの内訳をここに書く |
| recorded_at | TIMESTAMPTZ DEFAULT now() | この記録が作られた日時 |
| version_of | UUID FK → lines | 修正の場合、元の行 |
| superseded_at | TIMESTAMPTZ | この行が無効化された日時 |

制約: `CHECK (from_node <> to_node)`

**バージョニング(修正・削除の扱い)**

物理削除も上書きもしない。修正は「元の行を `superseded_at` で無効化し、新しい行を作る」。
削除は「無効化のみ」。修正と削除を同じ機構で扱う。

| 列 | 意味 |
|---|---|
| `superseded_at` | この行が無効化された日時。NULL なら有効 |
| `version_of` | 修正の場合、置き換え元の行の id。新規作成なら NULL |

- 修正: 旧行に `superseded_at` を入れ、新行を `version_of = 旧行.id` で作る
- 削除: 旧行に `superseded_at` を入れるだけ。新行は作らない
- `version_of` を辿れば、その取引の全バージョンの履歴が復元できる

通常の集計は `WHERE superseded_at IS NULL` で最新版だけを見る。
`recorded_at` を条件に加えれば、過去のある時点で見えていた帳簿も再現できる(bi-temporal)。

```sql
-- 2026-07-01 時点で見えていた帳簿
WHERE recorded_at <= '2026-07-01'
  AND (superseded_at IS NULL OR superseded_at > '2026-07-01')
```

### labels / line_labels

**用途の分類は階層が担うため、ノードにラベルは付けない。**
ラベルは評価軸(消費/浪費/投資など)専用とし、取引側にのみ付ける。

同じ店でも「昼食を買った=消費」「衝動買い=浪費」と変わるため、評価はノードではなく取引に紐づく。

```
labels       (id, user_id, name)
line_labels  (line_id, label_id)   PK: 複合
```

複数ラベルを許可するため中間テーブル方式を採る。
中間テーブルにする理由: ラベル名を後から変更しても全行に反映され、表記ゆれを防げる。

これにより「外食のうち浪費が何%」のような、階層 × 評価軸の交差分析ができる。


### valuations(評価スナップショット)

運用商品など、取引がないのに残高が変わるノード用。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID PK | |
| user_id | UUID FK | |
| node_id | UUID FK → nodes | 対象ノード |
| valued_on | DATE NOT NULL | 観測日 |
| market_value | NUMERIC(14,2) NOT NULL | その時点の実際の残高 |
| recorded_at | TIMESTAMPTZ DEFAULT now() | |

**差額の仕訳生成**

単純な前回との差ではなく、**その間に記録された取引を差し引く**。そうしないと還元分を二重計上する。

```
前回観測 2026-05-31: 10,000
今回観測 2026-06-30: 10,545
期間中の帳簿上の変動(還元など): +460
説明できない差額: 10,545 - 10,000 - 460 = 85
→ lines に 1行追加: 運用損益(flow) → PayPay運用(asset)  85
```

**生成された仕訳の扱い**

`valuations` から生成した仕訳は、通常の `lines` の行として保存する。
ただし手入力の取引と区別する必要があるため、`from_node` が損益ノード
(`運用損益` など)であることで識別する。

過去の観測値を修正した場合、それ以降に生成した仕訳は `superseded_at` で無効化し、
新しい差額で再生成する。手入力の取引には触らない。

**未観測期間の扱い**

観測を忘れた期間の損益は、観測日にまとめて計上する。期間に均等配分してはならない
(存在しなかったデータを作り出す行為であり、一次情報の汚染にあたる)。
グラフでは未観測期間を点線で表現するなど、UI側で明示する。

---

## 3. ビュー

集計ロジックはすべてここに集約する。CLI と Web はこれを select するだけ。

| ビュー | 内容 |
|---|---|
| `v_lines` | 有効な取引のみ(`superseded_at IS NULL`)。ノード名を JOIN 済み |
| `v_node_balances` | ノード別の累積残高 |
| `v_daily_deltas` | ノード×日の増減。まずビュー、遅ければ MATERIALIZED VIEW に変更 |
| `v_cumulative` | ウィンドウ関数による累積カーブ(グラフ用) |
| `v_net_worth` | `node_type IN ('asset','liability')` の合計 |
| `v_node_suggestions` | 直近30日の使用頻度順(入力サジェスト用) |
| `v_node_tree` | (祖先, 子孫) の全組み合わせ。階層集計の基盤 |
| `v_node_paths` | 各ノードの表示用パス文字列(食費 > 外食 > マクドナルド) |
| `v_rollup_balances` | 各ノードの、子孫を含めた合計 |

**符号の扱いはここで吸収する。** `from`/`to` 形式なので、
あるノードにとってその行が増加か減少かの判定が必要になるが、
それを書くのは `v_daily_deltas` の中だけ。以降のビューはそれを参照する。

```sql
-- v_daily_deltas の中核
SELECT to_node   AS node_id, occurred_on,  amount AS delta FROM v_lines
UNION ALL
SELECT from_node AS node_id, occurred_on, -amount AS delta FROM v_lines
```

ビューには `security_invoker = true` を指定し、呼び出したユーザーの権限で
ベーステーブルの RLS が評価されるようにする。

### 階層の集計(ロールアップ)

記録は葉ノードにしか存在しないため、中間ノードの数値は子孫の合計として計算する。

```sql
-- v_node_tree: (祖先ノード, その子孫ノード) の全組み合わせ
-- 自分自身も含む(深さ0)ため、葉ノードも1行持つ
CREATE VIEW v_node_tree WITH (security_invoker = true) AS
WITH RECURSIVE tree AS (
  SELECT id AS ancestor_id, id AS descendant_id, 0 AS depth, user_id
  FROM nodes
  UNION ALL
  SELECT t.ancestor_id, n.id, t.depth + 1, n.user_id
  FROM nodes n
  JOIN tree t ON n.parent_id = t.descendant_id
)
SELECT * FROM tree;
```

`user_id` を保持することで、`security_invoker` 経由で RLS が正しく効く。
これを省くと他人のノードが混ざる。

```sql
-- v_rollup_balances: 自分と子孫を合わせた合計
CREATE VIEW v_rollup_balances WITH (security_invoker = true) AS
SELECT t.ancestor_id AS node_id, SUM(d.delta) AS balance
FROM v_node_tree t
JOIN v_daily_deltas d ON d.node_id = t.descendant_id
GROUP BY t.ancestor_id;
```

「食費の合計」は、食費配下のすべての葉(その他(食費)、マクドナルド、スーパーA など)
の合計として求まる。葉ノード自身の残高も、深さ0の行があるため同じビューで取れる。

再帰は必ずこのビューに閉じ込める。呼び出し側で `WITH RECURSIVE` を書かない。

---

## 4. 仕訳パターン

すべて2ノードで表現できることを確認済み。

### 通常の支出

```
財布(asset) → 食費 > 外食 > マクドナルド(flow)   800
```

### 振替

```
銀行(asset) → 財布(asset)   10,000
```

### クレジットカード

使った時と引き落とし時は別の出来事なので、2件に分ける。

```
5/10  クレカ(liability) → その他(食費)(flow)   3,000
6/27  銀行(asset)      → クレカ(liability)     3,000
```

これで発生ベース(いつ使ったか)と現金ベース(いつ口座から出たか)の両方が正しく集計できる。

### 給与

給与ノードを経由させれば控除も2ノードで表現できる。

```
給与 > バイト先A(flow) → 銀行(asset)              235,000
給与 > バイト先A(flow) → 控除 > 所得税(flow)        20,000
給与 > バイト先A(flow) → 控除 > 社会保険(flow)      45,000
```

`給与 > バイト先A` ノードの累積が、そのバイト先からの総支給額になる。

### ポイント還元

```
ポイント還元(flow) → PayPay運用(asset)   120
```

### 運用損益

`valuations` から自動生成される。

```
運用損益(flow) → PayPay運用(asset)   85
```

### 立て替え

立て替えは `asset`(返してもらう権利)。クレカと符号が逆なだけの同じ構造。

```
支払時: クレカ(liability)         → 立て替え > 友人A(asset)   3,000
返済時: 立て替え > 友人A(asset)   → PayPay運用(asset)         3,000
```

未返済の間、その金額は資産として純資産に正しく含まれる。
`立て替え` を中間ノードにして相手ごとに葉を作れば、
ノード一覧で「誰にいくら貸しているか」が残高として一目で分かり、回収漏れを検出できる。
相手を特定しない分は `立て替え > その他` に入れ、メモに書く。

### 割り勘(自分が全額払った)

```
財布(asset) → 外食 > マクドナルド(flow)   2,500   自分の負担分
財布(asset) → 立て替え > 友人A(asset)     2,500   相手の分
```

### ポイント併用払い

from が分かれるだけなので2件に分ける。

```
PayPay運用(asset) → 外食 > マクドナルド(flow)     500
財布(asset)      → 外食 > マクドナルド(flow)   1,000
```

---

## 5. UX方針

### 日を第一級の単位にする

ホーム画面は「今日」から始まり、その日の各ノードの増減が見える。
日をまたぐと自動的に次の日に移り、過去の日をタップすると内訳に展開される。

一般的な家計簿は「取引リスト」が主役だが、この設計では「日ごとの積み上げ」が主役。
クレカやアプリの明細が日付順に並ぶため、日次で突き合わせて差分を検出できる利点もある。

### 入力

- 「どこから・どこへ・いくら」の3項目
- `from` は財布か銀行かの2択がほとんど
- `to` は直近30日の使用頻度順で上位8件をボタン表示(`v_node_suggestions`)
- 階層を辿らせない。サジェストと検索で葉ノードに直接到達させる
- 木を辿る UI は、ノードの管理・整理のときだけ使う
- ノード数が増えるため、検索とサジェストが入力体験の生命線

### ノード詳細画面

1種類で全ノードに対応する。財布でもスーパーAでも同じ画面。

- ヘッダの数値: `asset`/`liability` は現在残高、`flow` は直近30日の増減
- 累積カーブのグラフ
- 期間差分
- 取引一覧

**ノード一覧は node_type でセクションを分ける。** 資産と支出先の累積を並べると誤読を招くため。

`flow` セクションは木構造で表示し、中間ノードには子孫を含めた合計を出す。
折りたたみ可能にして、普段は上位階層だけ見えるようにする。

### 記録漏れへの対処

完璧に記録しようとすると挫折する。日次の締めで実際の財布残高を入力し、
帳簿との差額を「不明(現金)」ノードへの流出として計上する仕組みを持つ。
これで残高は必ず合い、記録漏れの量も数値で見える。

---

## 6. 構築手順

既存の `transactions` / `accounts` / `categories` はデモデータのため、データ移行は行わない。
削除して新スキーマを一から構築する。

### 前提

Supabase CLI をプロジェクトに紐付けておく。

```bash
supabase login
supabase link --project-ref sicucuywqqcapbogfkoy
```

インフラは既存のものをそのまま使う(Supabase / GitHub / Vercel は連携済み)。
プロジェクト名 `budget-sandbox` は変更しない。Vercel のプロジェクト名を変えると
デプロイ URL が変わり、ホーム画面に追加したリンクが切れるため。

### 手順

1. マイグレーションを作成する
   - 旧テーブル(`transactions` / `accounts` / `categories`)を DROP
   - `nodes` / `lines` / `labels` / `line_labels` / `valuations` を作成
   - 階層の制約をトリガーとして実装(node_type 一致、循環禁止、葉ノードのみ、user_id 一致)
   - 全テーブルに RLS を有効化し、`auth.uid() = user_id` のポリシーを4種類ずつ設定
   - ビュー群を作成(すべて `security_invoker = true` を指定)
2. `supabase db push` で適用する
3. 型を生成する: `supabase gen types typescript --linked > packages/core/types.ts`
4. `packages/core` を実装する
5. `apps/cli` を新スキーマに切り替え、動作確認する
6. `apps/web` を切り替える

### 注意

- `auth.users` は削除しない。サインアップ済みのアカウントが入っており、
  消すとログインできなくなる
- 削除するのは自分で作った3テーブルのみ
- 中間テーブル(`line_labels`)の RLS は、
  親テーブル側の `user_id` を EXISTS で参照する形になる

---

## 7. 未確定・今後の課題

- **入力コストの実測**: 1日あたりの取引件数、ノードの実際の種類数、入力所要時間。
  実際に1ヶ月使うまで分からない。手入力だけで継続可能か確認してから
  CSVインポートの優先度を決める
- **CSVインポート**: 銀行・クレカの明細取り込み。店名からノードを推定する
  マッピングテーブル(`merchant_map`)が必要になる
- **node_type による分岐の監視**: コード内で `node_type` による分岐が3箇所を超えたら、
  分岐をデータとして持つ設計(`node_types` テーブル)に切り替える。
  統一抽象が破綻していないかの指標にする
- **評価スナップショットの通知**: 最終観測日からの経過日数を表示し、記録を促す仕組み
- **ラベルは拡張の枠として置く**: 消費/浪費/投資は例示であり固定値ではない。
  `labels` はユーザーが自由に追加・改名・削除できるテーブルとして実装し、
  特定のラベル名をコードやスキーマにハードコードしない
- **`v_daily_deltas` の実体化判断**: 行数が増えて体感が悪化したら
  MATERIALIZED VIEW に切り替える。呼び出し側は無変更で済む設計にしておく
